Wind is a mini rpg made in java

Go to the project directory
Run the following command in the terminal: java --module-path "Path to the javaFX lib" --add-modules javafx.controls,javafx.fxml -jar wind.jar
